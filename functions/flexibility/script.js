function sum (a, b) {
    return a + b
}

console.log(sum(7, 7, 9, 15))
 //when there is more than values than it was declared in function, it use the first ones declared  an discards the rest

//if it has less values than it was declared, than the ones missing becomes undefined
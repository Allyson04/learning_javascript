// functions are used to  organize codes of block

            // length and width are parameters or arguments
            // inserting data via variables

function calculateArea(length, width) {
    var area = length * width;



    //functions have 2 types: void (when data is processed and stored)
    //and return (when data is processed and returned to script)
    return area 
}

var area = calculate(length, width);
var length = prompt("Type length:");
var width = 5;


document.write("O terreno de "  + width + " de comprimento e " + length + " de largura possui " + area + " metros quadrados.")